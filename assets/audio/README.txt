Drop one file named exactly:

    song.mp3

in this folder to power the soundtrack section (one carefully-chosen
track, not a playlist). Then open index.html, find <section id="playlist">,
and edit the ".track-title" and ".track-sub" text to match your song.
